'use strict';

angular.module('nextBartApp')
    .service('CordovaListeners', [
        '$rootScope',
        function CordovaListeners($rootScope) {
            // AngularJS will instantiate a singleton by calling "new" on this function

        }
    ]);